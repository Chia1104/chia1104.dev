{
  "$schema": "https://json.schemastore.org/package",
  "name": "www-e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test:e2e": "PORT=3002 HOST=127.0.0.1 pnpm exec playwright test",
    "clean": "git clean -xdf .turbo node_modules coverage"
  },
  "devDependencies": {
    "start-server-and-test": "^2.0.0",
    "tsconfig": "workspace:*",
    "www": "workspace:*"
  }
}
