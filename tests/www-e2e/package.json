{
  "$schema": "https://json.schemastore.org/package",
  "name": "www-e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start:www": "npx turbo run start --filter www",
    "test:e2e": "npx playwright test",
    "clean": "git clean -xdf .turbo node_modules coverage"
  },
  "devDependencies": {
    "start-server-and-test": "^2.0.0",
    "tsconfig": "workspace:*",
    "www": "workspace:*"
  }
}
