{
  "$schema": "https://json.schemastore.org/package",
  "name": "www-e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:e2e": "pnpm exec playwright test",
    "clean": "git clean -xdf .turbo node_modules coverage .cache"
  },
  "devDependencies": {
    "@chia/tsconfig": "workspace:*",
    "start-server-and-test": "^2.0.10",
    "typescript": "catalog:",
    "www": "workspace:*"
  }
}
