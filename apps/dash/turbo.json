{
  "$schema": "https://turbo.build/schema.json",
  "tags": ["dash"],
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"],
      "env": [
        "S3_ACCESS_KEY_ID",
        "S3_SECRET_ACCESS_KEY",
        "S3_REGION",
        "S3_BUCKET_NAME",
        "S3_ENDPOINT"
      ]
    }
  }
}
