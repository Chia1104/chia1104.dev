"use client";

import type { FC } from "react";
import { Button } from "@nextui-org/react";

const NewPost: FC = () => {
  return <Button onPress={() => null}>Create post</Button>;
};

export default NewPost;
