{
  "$schema": "https://turbo.build/schema.json",
  "tags": ["www"],
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"],
      "env": [
        "SENTRY_ORG",
        "SENTRY_PROJECT",
        "SENTRY_AUTH_TOKEN",
        "SHA_256_HASH",
        "EDGE_CONFIG",
        "SPOTIFY_FAVORITE_PLAYLIST_ID",
        "SENTRY_AUTH_TOKEN",
        "SENTRY_ORG",
        "SENTRY_PROJECT",
        "ADMIN_ID",
        "BETA_ADMIN_ID",
        "SPOTIFY_FAVORITE_PLAYLIST_ID",
        "GH_PUBLIC_TOKEN"
      ]
    }
  }
}
