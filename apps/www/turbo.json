{
  "$schema": "https://turbo.build/schema.json",
  "tags": ["www"],
  "extends": ["//", "@chia/api", "@chia/utils"],
  "tasks": {
    "build": {
      "env": [
        "SENTRY_ORG",
        "SENTRY_PROJECT",
        "SENTRY_AUTH_TOKEN",
        "SHA_256_HASH",
        "EDGE_CONFIG",
        "SPOTIFY_FAVORITE_PLAYLIST_ID"
      ]
    }
  }
}
