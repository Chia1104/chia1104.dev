name: labeler
on:
  - pull_request

jobs:
  triage:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v4
        with:
          dot: true
          sync-labels: true
          configuration-path: are-you-sure-about-that.yml
