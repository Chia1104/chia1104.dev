{
  "$schema": "https://turbo.build/schema.json",
  "tags": ["@chia/db"],
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["DATABASE_URL", "DATABASE_URL_REPLICA_1", "*_DATABASE_URL"],
      "passThroughEnv": [
        "DATABASE_URL",
        "DATABASE_URL_REPLICA_1",
        "*_DATABASE_URL"
      ]
    },
    "db:generate": {},
    "db:push": {},
    "db:init": {}
  }
}
